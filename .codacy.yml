---
engines:
  biome:
    exclude_paths:
      - ".opencode/plugins/custom-tools.ts"
  eslint:
    # 誤検知（Non-serializable expression）を回避するため、
    # Qwik 関連のルールが誤って適用されている可能性があるエンジンから除外します。
    exclude_paths:
      - ".opencode/plugins/custom-tools.ts"
