# OpenCode Cursor Provider プラグイン 要件定義書 (Provider V2)

## 1. プロジェクトの背景と目的
現在、本プラグインは OpenCode の「カスタムツール (`cursor_prompt`)」として提供されており、OpenCode のメインLLM（Claude 等）がサブタスクとして Cursor を呼び出す設計となっている。
しかし、ユーザーからは「Cursor の高性能なモデル（例: `claude-3-7-sonnet` 等）を OpenCode のメインLLM（Provider）として直接利用したい」という強い要望がある。
最新の Cursor Headless SDK (`cursor/cookbook` 参照) により、ストリーミング応答やプログラマブルなエージェント制御が可能になったため、本プラグインをツールベースから**「OpenCode ネイティブな Provider プラグイン」**へと抜本的にアーキテクチャを刷新する。

## 2. スコープ
### 2.1 含まれるスコープ (In-Scope)
- OpenCode Plugin SDK の `ProviderHook` の実装。
- ユーザーが `opencode.jsonc` の `provider` セクションで Cursor モデルを指定できる仕組みの提供。
- OpenCode の標準的なチャット履歴（System, User, Assistant のメッセージ配列）を、Cursor SDK のコンテキストへ変換するトランスレータ層の開発。
- Cursor SDK からのストリーミング応答（Server-Sent Events 相当）を OpenCode (Vercel AI SDK 互換形式) へリアルタイムに中継する機能。
- Cursor のサブスクリプション機能（Premiumモデル等）への透過的なアクセス。

### 2.2 含まれないスコープ (Out-of-Scope)
- OpenCode の MCP (Model Context Protocol) ツール群を Cursor エージェント側に完全に同期・委譲する機能（フェーズ2以降で検討）。
- Cursor アカウントの OAuth 自動ログイン機能（既存の `CURSOR_API_KEY` の環境変数ベースの認証を継続使用する）。

## 3. 機能要件
1. **Provider 登録機能 (`ProviderHook`)**
   - OpenCode 起動時に `Cursor` プロバイダーとして自身を登録する。
   - サポートするモデル一覧（例: `cursor/composer-2`, `cursor/claude-3-7-sonnet`, `cursor/gpt-4o` など）を OpenCode 側に提示する。
2. **メッセージ変換機能 (Message Translator)**
   - OpenCode から渡される履歴配列 (`UserMessage`, `Part` 等) を解析し、Cursor SDK の初期コンテキストやプロンプト指示として最適化して結合・マッピングする。
3. **ストリーミング推論 (Streaming Generation)**
   - Cursor SDK の AsyncGenerator/Stream インターフェースを利用し、生成途中のテキストチャンクを OpenCode の UI へリアルタイムにストリーミング返却する。
4. **ツール機能の非推奨化 / 削除**
   - 新規アーキテクチャへの移行に伴い、旧来の `cursor_prompt` ツール機能は非推奨化、あるいは Provider 内の実装と競合しない形に整理する。

## 4. 非機能要件
1. **パフォーマンスとレスポンス**
   - ストリーミングの遅延を最小限に抑えるため、変換層のオーバーヘッドを極小化する。
2. **エラーハンドリングと安定性**
   - Cursor 側のレート制限 (Rate Limit)、認証エラー、ネットワーク切断を適切に捕捉し、OpenCode の UI 上でユーザーに分かりやすいエラーメッセージとして表示する。
3. **セキュリティ**
   - `CURSOR_API_KEY` およびプロンプトの送受信内容は、機微情報としてログ（`client.app.log`）にはマスクして出力する（現行仕様を踏襲）。

## 5. アーキテクチャ設計案：ステート管理戦略

OpenCode の Provider は「毎ターン全履歴を送信（ステートレス）」する仕様だが、Cursor SDK の Agent は「インスタンスごとに状態を持つ（ステートフル）」というギャップが存在する。これを解決するため、**「ハッシュベースのAgentプーリング（差分送信） ＋ フルプロンプトへのフォールバック」**のハイブリッド戦略を採用する。

1. **履歴のハッシュ化とプーリング (最適化パス / B案の応用)**
   - Provider は毎ターン受け取った `messages` 配列のうち、最新の発言を除く「過去の履歴部分」のハッシュ値を計算する。
   - プラグイン内のメモリ上の Agent プール（Map等）に、そのハッシュと一致する既存の Cursor Agent インスタンスが存在する場合、「同じ会話の順当な続き」と判定する。
   - この場合、既存の Agent を再利用し、最新の `user` メッセージのみを `agent.send()` で差分送信する。これにより Agent の都度起動コストを抑え、コンテキストを完全に維持する。

2. **ステートレスなフルプロンプト送信 (フォールバック / A案)**
   - ハッシュが一致しない場合（新規チャット時、ユーザーが過去の会話を編集・分岐した場合、あるいはAgentがメモリからパージされた場合）、新たな Agent インスタンスを `Agent.create()` で生成する。
   - これまでの `messages` 配列（Systemプロンプトを含む過去の全履歴）を、XMLタグ（`<system>`, `<user>`, `<assistant>` 等）を用いて1つの統合されたプロンプト文字列として整形・圧縮し、初回リクエストとして `agent.send()` に投下する。
   - Agent の起動コストは発生するものの、あらゆるチャット操作（分岐やリトライ）において確実に会話を成立させるための堅牢なフォールバックとして機能する。

3. **Agent プールのライフサイクル管理 (LRU方式 / A案)**
   - 無限に Agent が増殖しリソースを圧迫するのを防ぐため、LRU (Least Recently Used) キャッシュアルゴリズムを採用し、プールの最大保持数を固定値（上限 8 個）に制限する。
   - 上限到達後に新たな Agent をプールに登録する必要が生じた場合、最も長期間アクセスされていない（古い）Agent を退避（削除）する。
   - 退避時およびプロセス終了時にはリソース解放のため `agent.close()` を呼び出すが、クローズ処理がハングアップして後続処理をブロックするのを防ぐため、`Promise.race` 等を用いてタイムアウト（5秒）を設定し、強制的に破棄処理を打ち切る安全設計とする。

4. **LLMパラメータのギャップとフォールバック (Warn-Once / B案)**
   - OpenCode は `chat.params` 経由で `temperature`, `topP`, `topK`, `maxOutputTokens` 等のパラメータを要求するが、Cursor SDK (`agent.send()`) はこれらをサポートせずモデルごとの最適値で内部制御している。
   - このギャップを埋めるため、ユーザーがデフォルト以外の値を指定してきた場合は「初回検出時のみ `log.warn` を出力」し、それ以降はログノイズを防ぐため黙殺（無視）する仕様とする。
   - これにより、運用者が「設定した値が反映されていない」ことに気付ける親切さを保ちつつ、毎ターンの不要なエラーログを防ぐことができる。

5. **Provider ID・命名規則とデフォルトモデル (A + i 案)**
   - `ProviderHook.id`（OpenCode 内での一意識別子）はシンプルに `"cursor"` とする。
   - モデルの命名規則は、Cursor SDK のネイティブ識別子をそのまま使用する（例: `composer-2`, `claude-3-7-sonnet`, `gpt-4o`）。これにより、ユーザーは `opencode.jsonc` で `cursor/composer-2` のように直感的かつ公式ドキュメントと一致する形で指定可能になる。
   - デフォルトモデルは既存実装を踏襲し `"composer-2"` を採用する。推論能力とコスト、そして何より Cursor のサブスクリプションを未契約のユーザーでも利用できるというバランスを考慮した設定とする。

## 6. 移行・開発フェーズ (マイルストーン)
- **Phase 1: 基礎検証 (PoC)**
  - Cursor Headless SDK を用い、CLI 上でストリーミングと会話履歴保持が実装可能か単体検証する。
- **Phase 2: ProviderHook の実装**
  - プラグインのコアを `tool` から `provider` に書き換え、OpenCode 上でモデルとして認識・選択できるようにする。
- **Phase 3: トランスレータ & ストリーミングの実装**
  - メッセージ変換ロジックとストリーミング中継層を実装し、エンドツーエンドでのチャットを成立させる。
- **Phase 4: テスト・ドキュメント更新**
  - エラーハンドリングの網羅、`README.md` および設定ガイドの更新を行い、リリースする。
